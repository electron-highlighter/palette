{
  "name": "electron-highlighter-palette",
  "version": "1.0.0",
  "private": true,
  "description": "Canonical palette + terminal config generator for Electron Highlighter Day",
  "license": "MIT",
  "scripts": {
    "build": "node generate.js",
    "test": "node --test"
  }
}
